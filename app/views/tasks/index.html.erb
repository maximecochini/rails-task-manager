<h1>My tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title.capitalize, task_path(task.id) %></li>
  <% end %>
</ul>

<hr>

<%= link_to new_task_path do %>
  <span>+ Add a new task</span>
<% end %>